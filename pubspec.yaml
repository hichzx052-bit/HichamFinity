name: hicham_finity
description: "HichamFinity — أداة البث الاحترافية لتيك توك"
publish_to: 'none'
version: 2.0.0+2

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  web_socket_channel: ^3.0.0
  google_fonts: ^6.1.0
  flutter_tts: ^4.2.0
  file_picker: ^9.2.0
  shared_preferences: ^2.2.2
  flutter_local_notifications: ^19.0.0
  video_player: ^2.9.2
  wakelock_plus: ^1.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
